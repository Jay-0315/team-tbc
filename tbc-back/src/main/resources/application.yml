server:
  port: 8080
  error:
    include-message: ALWAYS
    include-binding-errors: ALWAYS
    include-stacktrace: ALWAYS
  servlet:
    session:
      cookie:
        secure: false
        same-site: none
        http-only: true
  tomcat:
    threads:
      max: 200
  compression:
    enabled: true

spring:
  application:
    name: tbc
  jackson:
    time-zone: UTC

  datasource:
    url: jdbc:mysql://57.180.2.19:13306/tbc_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: tbc
    password: EjrqhR21!

  jpa:
    hibernate:
      ddl-auto: none   # 스키마 자동 변경 비활성화
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  sql:
    init:
      mode: never
  profiles:
    group:
      local: local

logging:
  level:
    org.springframework.web.socket: DEBUG
    org.springframework.web.socket.server: DEBUG
    org.hibernate.SQL: warn

jwt:
  secret: CHANGE_ME_TO_A_LONG_RANDOM_SECRET_64+_CHARS
  access-expiration-minutes: 15
  refresh-expiration-days: 14

springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /v3/api-docs
  override-with-generic-response: false

app:
  cookie:
    secure: true
    same-site: None
    access-name: access
    refresh-name: refresh
  seed: false
  seed.always: false
  seed.reviews: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

chat:
  max-content-length: 1000
  max-qps-per-session: 10

---
spring:
  config:
    activate:
      on-profile: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
