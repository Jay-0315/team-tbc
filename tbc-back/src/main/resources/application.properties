# --- DB ---
spring.datasource.url=jdbc:mysql://192.168.219.70:3306/tbc_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=tbc
spring.datasource.password=EjrqhR21!

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=never

# --- JWT ---
jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_64+_CHARS
jwt.access-expiration-minutes=15
jwt.refresh-expiration-days=14

# --- Cookie ---
app.cookie.secure=true
app.cookie.same-site=None
app.cookie.access-name=access
app.cookie.refresh-name=refresh

# --- Error Handling ---
server.error.include-message=ALWAYS
server.error.include-binding-errors=ALWAYS
server.error.include-stacktrace=ALWAYS

# --- Security Headers ---
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.http-only=true

# CORS
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
